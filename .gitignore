# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Node.js dependencies
/node_modules
/.pnp
.pnp.js

# Node.js specific logs and diagnostic reports
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log* # For monorepos (e.g., Lerna, Nx)
.report/ # Node.js diagnostic reports

# Build outputs
/build
/dist # Another common output folder name
/out # Another common output folder name
/coverage # Testing output

# Environment variables
.env # Generic .env file
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and Editor specific files
.DS_Store # macOS specific
.vscode/ # VS Code specific settings/files
.idea/ # JetBrains IDE (IntelliJ, WebStorm, etc.) specific files
*.sublime-project
*.sublime-workspace

# Temporary files and caches
.cache/ # Various build tools or dev servers might use this
.tmp/ # Generic temporary files directory
.thumbnails/ # If your app generates local image thumbnails

# Firebase local emulator and debug files
.firebase/ # Local Firebase emulators or config cache
firebase-debug.log
firebase-debug.*.log

# Runtime/Security files (often locally generated)
*.pem # Private key files
*.pub # Public key files
*.crt # Certificate files

# Storybook (if you use it)
storybook-static/

# Other miscellaneous files
# Add any other files or directories that are generated locally
# and should not be committed to version control.